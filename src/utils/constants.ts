// Hardcoding for now, but these will be dynamic based on user's location

const LOCALE = 'en';

const COUNTRY = 'US';

const CURRENCY = 'USD';

export const Constants = {
	LOCALE,
	COUNTRY,
	CURRENCY,
};
